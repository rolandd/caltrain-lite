{
  "name": "worker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "test": "tsx test/test-worker.ts",
    "check": "tsc --noEmit",
    "bench:gtfs-rt": "tsx bench/gtfs-rt.ts",
    "fetch:fixtures": "tsx fetch-fixture.ts",
    "check:endpoints": "tsx check-endpoints.ts",
    "lint": "pnpm -w run lint -- worker",
    "format": "pnpm -w run format -- worker",
    "format:check": "pnpm -w run format:check -- worker"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "pbf": "^5.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "@types/node": "^25.3.0",
    "tinybench": "^6.0.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
