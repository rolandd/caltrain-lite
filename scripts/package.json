{
  "name": "@transit/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "parse": "tsx parse-gtfs.ts",
    "test": "vitest run",
    "lint": "cd .. && npm run lint -- scripts",
    "format": "cd .. && npm run format -- scripts",
    "format:check": "cd .. && npm run format:check -- scripts"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
