{
  "name": "@transit/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx generate-schedule.ts",
    "parse": "tsx parse-gtfs.ts",
    "validate": "tsx validate-schedule.ts",
    "realtime:snapshot": "tsx generate-realtime-snapshot.ts",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "lint": "pnpm -w run lint -- scripts",
    "format": "pnpm -w run format -- scripts",
    "format:check": "pnpm -w run format:check -- scripts"
  },
  "dependencies": {
    "csv-parse": "^7.0.1",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
